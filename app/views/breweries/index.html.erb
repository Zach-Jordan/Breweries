<h1 class="mt-3">All Breweries</h1>

<%= form_with(url: breweries_path, method: "get", class: "mt-3") do %>
  <div class="form-group">
    <%= text_field_tag(:query, params[:query], placeholder: "Search by name", class: "form-control") %>
  </div>
  <%= submit_tag("Search", class: "btn btn-primary") %>
<% end %>

<table class="table mt-3">
  <thead class="thead-light">
    <tr>
      <th scope="col">Name</th>
      <th scope="col">Address</th>
      <th scope="col">Phone</th>
      <th scope="col">Website</th>
    </tr>
  </thead>
  <tbody>
    <% @breweries.each do |brewery| %>
      <tr>
        <td><%= link_to brewery.name, brewery_path(brewery) %></td>
        <td><%= brewery.address %></td>
        <td><%= brewery.phone %></td>
        <td><%= brewery.website %></td>
      </tr>
    <% end %>
  </tbody>
</table>